# cPanel-এ ডিপ্লয় করার নিয়ম

এই সাইটটা এখন **স্ট্যাটিক এক্সপোর্ট** হিসেবে কনফিগার করা — মানে বিল্ড করলে শুধু plain HTML/CSS/JS ফাইল তৈরি হয়, কোনো Node.js সার্ভার লাগবে না। যেকোনো সাধারণ শেয়ার্ড হোস্টিং (cPanel/Apache) এ আপলোড করলেই চলবে।

## ধাপ ১ — নিজের কম্পিউটারে বিল্ড করুন

```bash
npm install
npm run build
```

বিল্ড শেষ হলে প্রজেক্টের ভেতরে একটা **`out/`** ফোল্ডার তৈরি হবে — এটাই আসল ওয়েবসাইট (plain HTML/CSS/JS)। `.htaccess` ফাইলটাও অটোমেটিক এই ফোল্ডারে চলে আসবে (`public/.htaccess` থেকে কপি হয়)।

## ধাপ ২ — cPanel-এ আপলোড করুন

1. cPanel → **File Manager** খুলুন, `public_html` (বা যে ডোমেইন/সাবডোমেইনে সাইট বসাবেন তার রুট ফোল্ডার) এ যান।
2. `out/` ফোল্ডারের **ভেতরের সবকিছু** (ফোল্ডার নিজে না, ভেতরের ফাইলগুলো — `index.html`, `_next/`, `.htaccess` ইত্যাদি) zip করে আপলোড করুন, তারপর "Extract" করুন। অথবা FTP/FileZilla দিয়ে সরাসরি আপলোড করতে পারেন।
3. নিশ্চিত করুন `.htaccess` ফাইলটা `public_html`-এ আছে (এটা hidden ফাইল, File Manager-এ "Show Hidden Files" অন করতে হতে পারে)। **এই ফাইলটা ছাড়া প্রোডাক্ট পেজ রিফ্রেশ করলে 404 দেখাবে** — তাই এটা মিস করবেন না।

## ধাপ ৩ — চেক করুন

সাইটের হোমপেজ, একটা প্রোডাক্ট পেজে ঢুকে রিফ্রেশ দিয়ে, আর `/shop` পেজ — সবগুলো ঠিকঠাক লোড হচ্ছে কিনা দেখুন।

## গুরুত্বপূর্ণ নোট

- **Supabase URL/Key**: এগুলো `lib/supabase.ts`-এ আগে থেকেই কোডে বসানো আছে (publishable/anon key, ক্লায়েন্ট সাইডে থাকাটাই স্বাভাবিক — এটা কোনো নিরাপত্তা সমস্যা না)। আলাদা করে `.env` সেটআপের দরকার নেই।
- প্রতিবার প্রোডাক্ট/ব্যানার আপডেট করলে ওয়েবসাইট **অটোমেটিক** আপডেট হবে (এটা সরাসরি Supabase থেকে লাইভ ডেটা টানে) — নতুন করে বিল্ড/আপলোড করা লাগবে না। শুধু কোডে (ডিজাইন, টেক্সট, ফিচার) পরিবর্তন আনলে আবার ধাপ ১-২ রিপিট করতে হবে।
- যদি ডোমেইনের বদলে সাবফোল্ডারে বসান (যেমন `example.com/shop/`), তাহলে `.htaccess`-এর `RewriteBase /` লাইনটা `RewriteBase /shop/` করে দিতে হবে।
